-- we can get the date of closest Monday > using Week, first day of current month > using month , first day of current year > using year

select current_date,date_trunc('day', current_date), date_trunc('week', current_date), date_trunc('month', current_date),date_trunc('year', current_date)
